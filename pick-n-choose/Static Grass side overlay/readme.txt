This is the default side of grass blocks (pre-Beta 1.5). It is not affected by the biome temperature.
It was possible to have it with "Graphics: Fast" until version 1.7.10.
To revert back to the default side grass texture, copy the files from the Revert folder to the root of the resource pack.